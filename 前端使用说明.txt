登录：alice+123,密码区\n可以直接发送
主界面：左侧：用户数据库结构，单击可展开，除数据库（用ID命名，可以认为用户和数据库是一个东西）外的
列名和表名双击可触发自动查询；
中间上部为输入区，默认命令行式输入，按“;”触发发送；上方模式可切换文本模式，允许多语句同时输入并发送；
右侧为历史记录条目区，点击条目可显示该条目查询结果；中间下部为输出区，显示查询结果；

刷新当前：再次执行当前条目，不论原来是否失败
刷新全局：再次执行历史记录中的所有条目，不论原来是否失败
清空：清空历史记录

文本模式下
上方-文件：
新建：询问保存后清空
打开：询问保存后另打开
另存为：另存
关闭登录：（目前只能跳回登录，不能重登）

